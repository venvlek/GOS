Put your logo file here, named exactly:

    logo.jpg

It will then show up:
  - in the app header (src/components/ui/Logo.jsx)
  - as the browser tab favicon (index.html)

No code changes needed after you drop the file in — just restart `npm run dev`
(or hard-refresh) if the browser cached the old favicon.
